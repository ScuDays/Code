/*
#include <ncurses.h>
#include <cstdio>

int main() {
    initscr(); // 初始化ncurses库
    cbreak(); // 禁用行缓冲
    noecho(); // 禁用字符回显
    keypad(stdscr, TRUE); // 启用特殊键

    int ch = getch();
    if (ch == ' ')
    {
        printf("asadsda");
    }

    endwin(); // 清理和关闭ncurses库
    return 0;
}
*/
#include <stdio.h>

int main(){

    printf(":asdasdasdasdasd");
}